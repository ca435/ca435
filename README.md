echo "# siteparargar-on" >> README.md 
  git init 
  git add README.md 
  git commit -m "primeiro commit" 
  git branch -M main 
  git remote add origin https://github.com/ca435/siteparargar-on.git
   git push -u origem principal
